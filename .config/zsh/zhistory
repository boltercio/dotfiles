echo "hello im z0mbi3"
\
gtk3 (libgdk-3, libgtk-3)\
gtk-layer-shell (only on Wayland)\
pango (libpango)\
gdk-pixbuf2 (libgdk_pixbuf-2)\
libdbusmenu-gtk3\
cairo (libcairo, libcairo-gobject)\
glib2 (libgio, libglib-2, libgobject-2)\
gcc-libs (libgcc)\
glibc\

sudo apt install libgdk-3-dev
sudo apt search libgdk-3
sudo apt search libgdk
sudo apt install libgdk-pixbuf-2.0-dev
dir
cd dotfiles
dir
batcat bspwm_debian_dot.sh
git clone https://github.com/elkowar/eww
cd eww
dir
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain none -y
sudo apt install curl
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain none -y
sudo apt install rustup
sudo apt search rustup
sudo apt autoremove --purge rust-all
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain none -y
sudo apt install cargo
cargo build --release --no-default-features --features x11
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain none -y
cargo build --release --no-default-features --features x11
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
cargo build --release --no-default-features --features x11
sudo apt autoremove --purge rustc
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -y
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s - - -y
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
cargo build --release --no-default-features --features x11
dir
cd Repos
dir
cd bspdotfiles
dir
./bspwm_debian_dot.sh
dir
cd Repos/
dir
cd dotfiles
dir
cd eww
dir
cargo build --release --no-default-features --features x11
sudo apt search gdk | grep -C 2 3
sudo apt search gdk | grep -C 2 dev
sudo apt search gdk | grep -C 1 dev
sudo apt install libgtk-3-dev
cargo build --release --no-default-features --features x11
apt search libdbusmenu
sudo apt install libdbusmenu-glib-dev
cargo build --release --no-default-features --features x11
sudo apt install libdbusmenu-gtk3-dev
cargo build --release --no-default-features --features x11
sudo install -m 755 "$HOME/eww/target/release/eww" -t /usr/bin/
sudo install -m 755 target/release/eww -t /usr/bin/
dir
git clone https://github.com/kolbusa/stalonetray.git
cd stalonetray
dir
apt search stalonetray
dir
rm -rf dotfiles
dir
cd Repos
dir
cp bspdotfiles/bspwm_debian_dot.sh ../Scripts/installDots
dir
cd Scripts
dir
batcat installDots
dir
cd re
cd Repos
dir
rm -rf bspdotfiles
rm -rf dotfiles
dir
cp ../Scripts/installDots installDots
dir
ls -l
micro .zshrc
dir
dir Repos
mkdir installDots
mv installDots installDots
dir
mv installDots Repos
cp Repos/installDots installDots
dir
mv installDots Repos
dir installDots
rm -rf Repos/installDots
dir
mv installDots Repos
dir
cd Repos
dir
ce installDots
cd installDots
dir
git init
rm -rf .git
dir
git remote add origin https://github.com/boltercio/installDots.git\
git branch -M main\
git push -u origin main
git init
rm -rf .git
git config --globa init.defaultBranch main
git init
git remote add origin https://github.com/boltercio/installDots.git\
git branch -M main\
git push -u origin main
git add .
git commit -m "Script inicial"
git config --global init.defaultBranch main
git config --global user.email "juanantonio.detena@gmail.com"
git config --global user.email "Juan Antonio de Tena (A.K.A Bolters)"
git status
git reset
dir
git status
git add .
git commit -m "Script inicial"
git config --global user.name "Juan Antonio de Tena (A.K.A Bolters)"
git config --global user.email "juanantonio.detena@gmail.com"
git commit -m "Script inicial"
git remote add origin https://github.com/boltercio/installDots.git
git push -u origin main
dir
cd Scripts
dir
micro github_tocker
mv github_tocker github_tocken
dir
sudo apt update && sudo apt upgrade -y
